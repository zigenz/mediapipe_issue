<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.opticalintelligence.ett">
    <!-- For using the camera -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OpticalIntelligenceETT">
        <activity android:name=".ui.launcher.LauncherActivity"/>
        <activity
            android:name=".ui.login.LoginActivity"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.eye_test.EyeTestActivity"/>

        <meta-data
            android:name="cameraFacingFront"
            android:value="${cameraFacingFront}" />
        <meta-data
            android:name="binaryGraphName"
            android:value="${binaryGraphName}" />
        <meta-data
            android:name="inputVideoStreamName"
            android:value="${inputVideoStreamName}" />
        <meta-data
            android:name="outputVideoStreamName"
            android:value="${outputVideoStreamName}" />
        <meta-data
            android:name="flipFramesVertically"
            android:value="${flipFramesVertically}" />
        <meta-data
            android:name="converterNumBuffers"
            android:value="${converterNumBuffers}" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>