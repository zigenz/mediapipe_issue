<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_gradient"
    tools:context=".ui.eye_test.EyeTestActivity">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingHorizontal="@dimen/activity_padding_horizontal"
        android:paddingTop="@dimen/activity_padding_vertical">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/header_block"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/back_btn"
                android:layout_width="@dimen/top_button_diameter"
                android:layout_height="@dimen/top_button_diameter"
                android:background="@drawable/gradient_button"
                android:includeFontPadding="false"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                android:text=""
                android:textAlignment="center"
                app:backgroundTint="@null"
                app:cornerRadius="@dimen/top_button_corner_radius"
                app:icon="@drawable/ic_back_arrow"
                app:iconGravity="textEnd"
                app:iconPadding="0dp"
                app:iconTint="@color/white"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginLeft="@dimen/top_button_spacing"
                android:fontFamily="@font/poppins"
                android:text="Eye Test"
                android:textColor="@color/white"
                android:textFontWeight="400"
                android:textSize="@dimen/font_size_header"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toEndOf="@id/back_btn"
                app:layout_constraintTop_toTopOf="parent" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/logout_btn"
                android:layout_width="@dimen/top_button_diameter"
                android:layout_height="@dimen/top_button_diameter"
                android:background="@drawable/gradient_button"
                android:includeFontPadding="false"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                android:text=""
                android:textAlignment="center"
                app:backgroundTint="@null"
                app:cornerRadius="@dimen/top_button_corner_radius"
                app:icon="@drawable/ic_logout"
                app:iconGravity="textEnd"
                app:iconPadding="0dp"
                app:iconTint="@color/white"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:strokeColor="@color/black"
                app:strokeWidth="1dp" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/help_btn"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="@dimen/top_button_diameter"
                android:layout_height="@dimen/top_button_diameter"
                android:layout_marginEnd="@dimen/top_button_spacing"
                android:background="@drawable/gradient_button"
                android:includeFontPadding="false"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                android:shadowColor="@color/black"
                android:text=""
                android:textAlignment="center"
                android:theme="@style/Theme.MaterialComponents"
                app:backgroundTint="@null"
                app:cornerRadius="@dimen/top_button_corner_radius"
                app:icon="@drawable/ic_help"
                app:iconGravity="textEnd"
                app:iconPadding="0dp"
                app:iconTint="@color/white"
                app:layout_constraintEnd_toStartOf="@+id/logout_btn"
                app:layout_constraintTop_toTopOf="parent" />

        </androidx.constraintlayout.widget.ConstraintLayout>

        <FrameLayout
            android:id="@+id/context"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="30dp"
            android:layout_marginBottom="10dp"
            android:orientation="vertical"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toBottomOf="@id/header_block">

        </FrameLayout>

        <FrameLayout
            android:id="@+id/preview_display_layout"
            android:layout_width="fill_parent"
            android:layout_height="0dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHeight_percent="0"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <TextView
                android:id="@+id/no_camera_access_view"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent"
                android:gravity="center"
                android:text="@string/no_camera_access" />
        </FrameLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.constraintlayout.widget.ConstraintLayout>